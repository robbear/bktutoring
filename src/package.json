{
  "name": "bktutoring",
  "version": "1.0.0",
  "description": "bearmankorthtutoring.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/robbear/bktutoring"
  },
  "author": "robbear@hyperfine.com",
  "dependencies": {
    "elix": "6.0.2"
  },
  "devDependencies": {},
  "engines": {
    "node": "10.16.0"
  },
  "scripts": {
    "build": "rm -rf ../build && mkdir -p ../build && cp -r ./ ../build/src"
  }
}
